# Variáveis de Ambiente - Copie este arquivo para .env.local e preencha os valores

# Senha do administrador (OBRIGATÓRIO)
# Configure uma senha forte antes de usar em produção
ADMIN_PASSWORD=

# PostgreSQL (apenas para produção na Vercel)
# Configure estas variáveis apenas se estiver usando PostgreSQL
# Na Vercel, essas variáveis são configuradas automaticamente quando você cria o banco Postgres
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# Nota: Para desenvolvimento local, o projeto usa SQLite automaticamente
# Não é necessário configurar POSTGRES_URL localmente
